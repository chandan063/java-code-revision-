import java.util.*;
//finding the time -complexity of the simple loop
for(int i=0;i<n;i++){
    //constant work
    //is done in this loop

    //to find the TC we need to find the operation happening here 
    //means how many time the loop runs
    // remove the constant 
    O(n)==time-complexity;
}

// NESTED LOOP

for(int i=0;i<n;i++){
    for(int j=i+1;j<n;j++){
         //constant work
        //is done in this loop
        TC=O(n^2);
    }
}